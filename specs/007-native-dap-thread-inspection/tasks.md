# Tasks — Native DAP thread inspection

| ID | Type | Blocked by | Acceptance checkpoint |
|---|---|---|---|
| B1-T01 | Explore | — | Verify vendored `ThreadsRequest`/`ThreadsResponse`, actual controlled-adapter behavior, and the existing native `CallToolResult` serializer: `structuredContent` is the application object, one text `content` item carries its identical JSON, and `isError` denotes application errors. Record that the Python `data`/`state` envelope is not a native result contract, that `threads` has no standard capability gate, and exact inclusive 256/1,024/262,144 bounds. |
| B1-T02 | Test | B1-T01 | Add RED `ModernMcp/*Threads*Tests.cs` process tests for the published `string`/`minLength: 1` schema, a missing arguments object, omitted `debugSessionId`, closed input (`non_string`, `empty`, `whitespace_only`, and `extra_field`), opaque unavailable tokens, success, exact refusal/protocol-error envelopes, no raw DAP body, and catalog order. For every `get_threads` application outcome, assert `resultType:"complete"`; that the sole text `content` item is byte-for-byte identical to the native serialized `structuredContent` JSON; and `isError:false` only for `threads_success` and `isError:true` for each application error. Assert that `structuredContent` has neither Python `data`/`state` fields nor raw DAP envelope, `body`, or adapter-error fields. This same test owner measures the native MCP result serializer’s normalized `structuredContent` only: 262,144 UTF-8 bytes returns `threads_success`, while 262,145 returns `dap_threads_protocol_error`/`DAP_THREADS_PROTOCOL_ERROR`, no partial list or raw DAP field, and bounded cleanup. Add `SessionSlot` race tests for explicit stop, host shutdown, unusable-session eviction, timeout, protocol error, and a cleanup-triggering admitted call releasing its own lease before close-and-drain. Add `ReaderFailureWithoutInflightRequest_NotifiesRegistryAndEvictsToken` and `ReaderFailureWithInflightGetThreads_ClosesAdmissionDrainsAndEvictsToken`. |
| B1-T03 | Code | B1-T02 | Add only the internal typed thread operation/result, registry-owned `SessionSlot` admission lease, release-before-close-and-drain handoff, shared `StopTimeout` drain deadline, slot abort token linked into typed DAP write/wait, one close-task winner, and `StopAsync` followed by `DisposeAsync` in `finally` only after all leases settle. Add `NetCoreDbgSession` reader-failure registry notification, controlled adapter modes, and additive ten-tool MCP dispatch; unchanged B1-T02 tests become green. `SessionSlot` must remain lifecycle ownership, never a generic DAP proxy. |
| B1-T04 | Review | B1-T03 | S2 review using installed platform source `C:/Users/btf/.omp/profiles/nvmd-selfhost/plugins/cache/plugins/nvmd-ai___nvmd-ai___0.9.19/wiki/security-review.md`; write its durable receipt only to `.agent/runs/b1-native-thread-inspection/security-review.md` in the primary repository. Check opaque-token handling, framing/correlation, bounds/allocation, error redaction, closed admission and atomic token removal, one cleanup winner, release-before-close-and-drain, the shared deadline/forced-abort sequence, and `DisposeAsync` in the cleanup `finally`. Exercise each cleanup path—explicit stop, host shutdown, unusable-session eviction, timeout, protocol error, reader failure without in-flight work, delayed reader failure with an in-flight lease, hung `get_threads` at drain expiry, and a faulting `StopAsync`—for no self-deadlock, no transport use-after-dispose, no raw cleanup/DAP leakage, and no Python/cutover surface. |
| B1-T05 | Test | B1-T04 | Run focused native process/lifecycle gates and the exact installed-Python `PRODUCT_WORKS` non-selection journey in `specs/006-a1-local-preview/quickstart.md`, recording nonzero denominators. |

Granularity: T02 is RED behavior, T03 is one vertical code slice, T04 is independent security review, T05 is final observable proof. No task changes the Python route or release surfaces.

Fixture wiring for B1-T02/T03: `ModernMcp/*Threads*Tests.cs` selects each response mode through `ModernMcpStartOptions.FixtureConfiguration`; `DebugAdapter/NetCoreDbgSessionContractDriver.cs` owns `FixtureConfiguration` and `FixtureProcess.Create`, which forwards it as `CONTROLLED_DAP_OPTIONS`; `Fixtures/ControlledDapAdapter/Program.cs` owns `AdapterOptions.Parse` and `ControlledDapAdapter.HandleRequestAsync` mode behavior. `FixtureConfiguration.cs` does not exist.
