# Tasks — Native DAP thread inspection

| ID | Type | Blocked by | Acceptance checkpoint |
|---|---|---|---|
| B1-T01 | Explore | — | Verify vendored `ThreadsRequest`/`ThreadsResponse`, actual controlled-adapter behavior, and Python output/error ownership; record that `threads` has no standard capability gate and exact inclusive 256/1,024/262,144 bounds. |
| B1-T02 | Test | B1-T01 | Add RED modern/lifecycle tests named for the published `string`/`minLength: 1` schema, a missing arguments object, omitted `debugSessionId`, closed input (`non_string`, `empty`, `whitespace_only`, and `extra_field`), opaque unavailable tokens, success, exact refusal/protocol-error envelopes, no raw body, catalog order, 256/257, 1,024/1,025 UTF-8, and final-serialization boundaries. Add `SessionSlot` race tests for explicit stop, host shutdown, unusable-session eviction, timeout, protocol error, and a cleanup-triggering admitted call releasing its own lease before close-and-drain. Add `ReaderFailureWithoutInflightRequest_NotifiesRegistryAndEvictsToken` and `ReaderFailureWithInflightGetThreads_ClosesAdmissionDrainsAndEvictsToken`. |
| B1-T03 | Code | B1-T02 | Add only the internal typed thread operation/result, registry-owned `SessionSlot` admission lease and release-before-close-and-drain handoff, `NetCoreDbgSession` reader-failure registry notification, controlled adapter modes, and additive ten-tool MCP dispatch; unchanged B1-T02 tests become green. |
| B1-T04 | Review | B1-T03 | S2 review using installed platform source `C:/Users/btf/.omp/profiles/nvmd-selfhost/plugins/cache/plugins/nvmd-ai___nvmd-ai___0.9.19/wiki/security-review.md`; write its durable receipt only to `.agent/runs/b1-native-thread-inspection/security-review.md` in the primary repository. Check opaque-token handling, framing/correlation, bounds/allocation, error redaction, each cleanup-admission race, release-before-close-and-drain handoff, both reader-failure paths, and no Python/cutover surface. |
| B1-T05 | Test | B1-T04 | Run focused native process/lifecycle gates and the exact installed-Python `PRODUCT_WORKS` non-selection journey in `specs/006-a1-local-preview/quickstart.md`, recording nonzero denominators. |

Granularity: T02 is RED behavior, T03 is one vertical code slice, T04 is independent security review, T05 is final observable proof. No task changes the Python route or release surfaces.
