# Tasks — Typed native DAP step-over

| ID | Type | Blocked by | Acceptance checkpoint |
| --- | --- | --- | --- |
| B3-G0 | Review — architect FULL challenger | — | The recorded challenger result is `REVISE`; this repair does not satisfy the gate. Independently recheck this exact ADR/spec/plan/tasks packet before implementation and write one terminal `GO`, `REVISE`, or `RETHINK` verdict to root `.agent/runs/b3-native-step-over/g0-challenger.md`. Cover premise, existing B1/B2 leverage, typed-next versus first-non-exact-event rejection, live-session preservation, continue/generic/Python alternatives, narrow scope, source freshness, dependency claims, complexity/value, scope creep, assumptions, bias, one per-session active record, capability-independent stopped authority, and the closed-input/lifecycle trust boundary. |
| B3-T01 | Explore | B3-G0 | Write one source-backed finding to root `.agent/runs/b3-native-step-over/protocol-seam.md`. Record vendored DAP `next` required Int32 `threadId`, acknowledgement-only response, response-before-step-stop promise, other-thread resumption, optional `singleThread`/granularity exclusion, continued-event omission for implied continuation, stopped event’s optional thread id, and absence of vendor same-thread/event-request correlation. Map `DebugSessionRegistry`, `SessionSlot`, `NetCoreDbgSession` framing/pending/reader, the B2 gates, event-owned stopped authority, controlled fixture, and process/typed-session/schema seams. Confirm `Program.cs` native request timeout is 30 seconds. Distinguish Python’s `thread_id or current_thread_id` and `execute_and_wait` from B3’s non-imported behavior. |
| B3-T02 | RED Test | B3-T01 | Before production code, add focused failing native process, typed-session, lifecycle, and schema tests. Cover catalog placement and closed `{debugSessionId,threadId}` schema; every invalid-input/no-I/O branch; unavailable opaque token/no-I/O; explicit Int32 forwarding with only `next({threadId})`; no `singleThread`/granularity; all-stop/partial/missing stopped-authority semantics independent of delayed-stack capability; absent-authority refusal/no write; one active-record single-flight concurrent refusal/no-I/O; negative acknowledgement refusal; acknowledgement then later exact `stopped(reason:"step",same target)` success; complete structured-content/text identity; B2 frame-authority invalidation on admitted step and continued; held write/continued ordering; pre-ack and unrelated stopped lifecycle update/non-credit; missing/different target and non-step ignore then exact success or timeout; terminal before/after acknowledgement; one post-write 30-second deadline; prewrite cancellation and negative ack preserve; postwrite timeout/cancellation and protocol error release lease then drain; only the five closed result/schema variants. |
| B3-T03 | Code | B3-T02 | Implement only the additive typed native `step_over` tool and its required frozen schema/result variants. Reuse `DebugSessionRegistry` for public parsing/lease/normalization and `SessionSlot` for existing cleanup. In `NetCoreDbgSession`, implement the private typed `next` act→observe operation: established admission/state gates verify capability-independent stopped authority, reserve one nullable per-session active record or refuse with zero I/O, invalidate stopped/B2 call-stack/frame authority, register/write the correlated request, release admission after bytes commit, accept only its exact positive acknowledgement, and credit only a later exact `stopped(reason:"step",threadId)` event. Preserve all other lifecycle updates without settling the B3 waiter; terminals may settle. Use one 30-second post-write deadline. Negative acknowledgement/prewrite cancellation preserve; timeout/postwrite cancellation/protocol error release the lease then join `CloseAndDrainAsync`. No generic execution path, queue, SessionSlot exclusivity, or sixth public result/schema variant. |
| B3-T04 | Review — independent code and security | B3-T03 | Review the exact B3 candidate; B3-G0 remains a completed pre-code admission and is not reopened or replaced. B3 is S2: apply `C:/Users/btf/.omp/profiles/nvmd-selfhost/plugins/cache/plugins/nvmd-ai___nvmd-ai___0.9.19/wiki/security-review.md` and write the review to root `.agent/runs/b3-native-step-over/security-review.md`. Record the S1 dependency/CVE disposition, OWASP Top 10 review, .NET-specific `SecurityCodeScan` and `dotnet list package --vulnerable` dispositions, and closed input/output/redaction checks. Review opaque capability lookup, DAP framing/correlation, active-record lifetime, admission/write versus lifecycle ordering, capability-independent authority, one-record single-flight, ignored non-credit events, terminal/timeout/cancellation normalization, uncertain-act drain, raw-error redaction, and SessionSlot one-owner cleanup. Record each finding with a disposition; unresolved blocking findings prevent B3-T05. |
| B3-T05 | Test | B3-T04 | On the exact candidate, run `StepOverContractTests`, `ModernProtocolContractTests`, `GetThreadsContractTests`, `GetCallStackContractTests`, `NetCoreDbgSessionTests`, `CapabilityLifecycleContractTests`, `SessionSlotContractTests`, `SessionSlotLifecycleTests`, and `StructuredContentSchemaParityTests`; record every actual nonzero denominator in root `.agent/runs/b3-native-step-over/receipt.yaml`. Demonstrate the controlled customer path: live stopped explicit thread -> `step_over` -> only `next({threadId})` -> acknowledgement -> later exact step stop -> normalized result -> state query -> stop. Demonstrate invalid/no-I/O, unavailable token/no-I/O, no-authority/concurrent/negative-ack refusal, all-stop/partial authority, B2 invalidation, pre-ack/unrelated ignored non-credit, terminal, shared post-write timeout, prewrite/postwrite cancellation, and protocol-error cleanup. Retain the installed-Python `PRODUCT_WORKS` non-selection proof. |

## Fixture wiring

`ModernMcp/StepOverContractTests.cs` is the future public MCP contract owner. It selects fixture behavior through `ModernMcpStartOptions.FixtureConfiguration`; `ModernMcp/ModernMcpProcessDriver.cs` remains the official native MCP stdio entry. `DebugAdapter/NetCoreDbgSessionContractDriver.cs` continues to expose deterministic pending/write/lifecycle observation for typed-session tests. `Fixtures/ControlledDapAdapter/Program.cs` owns only the new `next` response mode, acknowledgement/event ordering, held-response release, exact/different/missing target stopped event, unrelated/pre-ack events, terminal, non-response, and postwrite-cancellation modes required for B3. Existing B2 fixture modes and behavior remain preserved. `CONTROLLED_DAP_OPTIONS` remains the fixture configuration transport.

## Granularity check

- **B3-G0** yields one durable independent design-admission verdict at `.agent/runs/b3-native-step-over/g0-challenger.md`, not code.
- **B3-T01** yields one durable source/protocol finding at `.agent/runs/b3-native-step-over/protocol-seam.md`.
- **B3-T02** yields RED observable contract evidence.
- **B3-T03** is one additive end-to-end implementation slice.
- **B3-T04** yields an independent S2 review at `.agent/runs/b3-native-step-over/security-review.md` with findings and dispositions.
- **B3-T05** yields exact-candidate customer-path and focused regression evidence at `.agent/runs/b3-native-step-over/receipt.yaml`.

Each ticket is independently meaningful and checkable. None absorbs continue, pause, step-in/out, breakpoints, generic DAP, UI, Python/default selection, release, or publication.

## Milestone closure

M1 closes only when the fresh B3-G0 recheck has a terminal disposition, B3-T01 has its durable source receipt, B3-T02 is demonstrated RED before B3-T03 turns it GREEN, B3-T04 has no unresolved blocking finding, and B3-T05 records every named exact-candidate nonzero-denominator check plus retained Python non-selection proof. The completed milestone is demoable but has `release_intent:none`; it must not trigger release work.
