# Tasks — Native DAP frame inspection

| ID | Type | Blocked by | Acceptance checkpoint |
|---|---|---|---|
| B2-T01 | Explore | — | Record the vendored DAP `stackTrace` request/response and `StackFrame` constraints from `docs/dap-protocol/specification.md` and `debugAdapterProtocol.json`: `threadId` Int32; paging inputs uint32; DAP permits omitted/zero all-frames while B2 rejects zero; response requires `body.stackFrames`; `totalFrames` is optional uint32; frame `id` is Int32, `line`/`column` are uint64; empty name is valid within 1,024 UTF-8 bytes; and absent source path, including sourceReference-only source, normalizes null/0/0 while raw sourceReference remains excluded. Verify `NetCoreDbgSession` initialize capability capture, `stopped`/`continued` handling, request-write/correlation seam, B1 `Program.cs` registry/`SessionSlot`, frozen schema, structured-content serializer, controlled fixture, and process-driver seams. Record that B2 must retain only an initialize-derived `supportsDelayedStackTraceLoading:true` session capability record, not a generic registry, and that Python is an oracle only: its thread fallback, retries, source-context read, and envelopes are excluded. |
| B2-T02 | RED Test | B2-T01 | Add failing focused modern process and typed-session/lifecycle tests before production code. Cover catalog order and closed input; explicit `threadId`; defaults 0/20; valid uint32 limits; rejected `levels:0`; no DAP I/O for invalid input or opaque unavailable token; initialize capability present success and omitted/false capability refusal with zero `stackTrace` writes; stopped success; explicit bounded page; source-null and sourceReference-only normalization; empty-name validity; optional `totalFrames`; one text item JSON-identical to structured content; all exact redacted refusal/protocol errors; no raw DAP data; response boundaries including 256 frames, per-field bytes, and 262,144/262,145 structured-content bytes. Extend the controlled fixture and drivers with explicit initialize-capability, `stackTrace`, held-response, and continued-at-admission modes. The continued-race test must prove either request write before continued under the prior opaque stopped epoch or refusal without a post-continued write; it must also prove no registry preflight. Reuse `SessionSlotLifecycleTests` patterns to prove protocol-error release-before-drain and no premature cleanup of an admitted call-stack lease. Extend frozen schema parity only for B2 variants. |
| B2-T03 | Code | B2-T02 | Implement only the additive typed native `stackTrace` operation and new MCP dispatch/catalog/result/schema variants required to turn B2-T02 green. In `NetCoreDbgSession`, capture only the initialize response's delayed-stack capability, and use one internal DAP operation/state gate for `stopped`/`continued` epoch mutation and typed call-stack admission through correlated request write. Do not expose or cache an epoch; when capability is absent or continued wins admission, return refusal with no `stackTrace` write. Keep the existing registry free of state/capability prechecks and use its `SessionSlot` lease/abort/close owner without a new registry or cleanup path. Normalize only id/name/source/line/column/optional totalFrames; accept bounded empty names and source objects lacking path, exclude raw sourceReference, bound bytes before publication, preserve refusal usability, and evict/drain protocol errors. Do not modify Python, selector, bridge/UI, scopes/variables, execution control, generic DAP support, release, or publication. |
| B2-T04 | Review | B2-T03 | Obtain independent review of the exact B2 candidate. The pending architect FULL challenger must produce a `GO`/`REVISE`/`RETHINK` verdict and tagged findings for this packet. B2 is S2: apply the installed platform `security-review` contract at `C:/Users/btf/.omp/profiles/nvmd-selfhost/plugins/cache/plugins/nvmd-ai___nvmd-ai___0.9.19/wiki/security-review.md`, covering closed input/token lookup, DAP request framing, initialize-capability/stopped admission, response bounds/redaction, continued-race behavior, and cleanup. Write durable security evidence only to root `.agent/runs/b2-native-frame-inspection/security-review.md`. The implementation review must inspect no ambient thread, no generic forwarder/capability registry, atomic request/state gate, DAP correlation, valid empty-name/sourceReference-only behavior, field and serialized bounds, no source read, same-epoch unpersisted frame ids, refusal usability, and release-before-existing-close-and-drain behavior. Any finding must be reconciled within the scoped B2 contract before final test. |
| B2-T05 | Test | B2-T04 | On the exact B2 candidate, run the focused B2 native process/typed-session/lifecycle/schema gates and, before or alongside them, run the current-candidate B1 baseline named `GetThreadsContractTests`, `SessionSlotContractTests`, `SessionSlotLifecycleTests`, `NetCoreDbgSessionTests`, and `StructuredContentSchemaParityTests`. Record each actual nonzero denominator; B1's pre-merge 52/52 receipt is not eligible evidence. Demonstrate the controlled native journey: start with delayed-stack capability, observe stopped state, `get_threads`, explicit `get_call_stack`, verify normalized page/no raw DAP content, verify session usability, then stop. Separately demonstrate capability-absent refusal/no-write and continued-race ordering. Replay the retained installed-Python `PRODUCT_WORKS` journey named in `specs/006-a1-local-preview/quickstart.md` as non-selection proof. Record exact candidate and denominators; no release, publication, tag, or selector action. |

## Fixture wiring

`ModernMcp/GetCallStackContractTests.cs` selects initialize-capability, held-response, and continued-at-admission response modes through `ModernMcpStartOptions.FixtureConfiguration`. `DebugAdapter/NetCoreDbgSessionContractDriver.cs` owns fixture configuration and `FixtureProcess.Create`, forwarding it through `CONTROLLED_DAP_OPTIONS`. `Fixtures/ControlledDapAdapter/Program.cs` owns option parsing, initialize capability response, stopped/continued event ordering, and `stackTrace` mode behavior. `ModernMcp/ModernMcpProcessDriver.cs` remains the official MCP stdio process entry. These are future source/test scopes only; this planning packet executes none of them.

## Granularity check

T01 yields one written ownership/protocol finding, T02 yields RED observable behavior, T03 is one additive end-to-end code slice, T04 yields an independent S2 and challenger verdict, and T05 yields current-candidate customer-facing proof. Each is independently checkable and none absorbs a scopes, variables, Python migration, selector, tag, publication, or release slice.