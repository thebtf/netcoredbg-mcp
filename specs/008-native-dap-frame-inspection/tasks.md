# Tasks — Native DAP frame inspection

| ID | Type | Blocked by | Acceptance checkpoint |
|---|---|---|---|
| B2-G0 | Admission recheck | — | Before any implementation resumes, obtain a fresh independent recheck of this corrected ADR/spec/plan/tasks packet. It must issue `GO`, `REVISE`, or `RETHINK` against all-thread/named-partial `continued` invalidation, request-local token-current response publication, strict partial-stop replacement, and safe-integer frame bounds. This repair supersedes the prior incomplete recheck and cannot itself satisfy `B2-G0`; no B2 implementation admission occurs until its terminal verdict. |
| B2-T01 | Explore | B2-G0 | Record the vendored DAP `stackTrace` request/response and `StackFrame` constraints from `docs/dap-protocol/specification.md` and `debugAdapterProtocol.json`: `threadId` Int32; paging inputs uint32; DAP permits omitted/zero all-frames while B2 rejects zero; response requires `body.stackFrames`; `totalFrames` is optional uint32; frame `id` is Int32; `line`/`column` are integers `0..9007199254740991` inclusive; empty name is valid within 1,024 UTF-8 bytes; and absent source path, including sourceReference-only source, normalizes null/0/0 while raw sourceReference remains excluded. Record lifecycle semantics: all-stop creates all-target eligibility; partial stop replaces every token with the supplied target only or none when its `threadId` is absent; continued omitted/true clears every token; continued false with named `threadId` invalidates only that target and preserves unrelated eligibility; continued false without `threadId` clears every token and retains no unknown token. Verify `NetCoreDbgSession` initialize capability capture, lifecycle handling, request-write/correlation seam, B1 `Program.cs` registry/`SessionSlot`, frozen schema, structured-content ceiling, and controlled-fixture hooks without modifying source or tests. |
| B2-T02 | RED Test | B2-T01 | Add failing focused modern process and typed-session/lifecycle tests before production code. Cover catalog order and closed input; explicit `threadId`; defaults 0/20; valid uint32 limits; rejected `levels:0`; no DAP I/O for invalid input or opaque unavailable token; initialize capability present success and omitted/false capability refusal with zero `stackTrace` writes; stopped success; explicit bounded page; source-null and sourceReference-only normalization; empty-name validity; optional `totalFrames`; one text item JSON-identical to structured content; all exact redacted refusal/protocol errors; no raw DAP data; response boundaries including 256 frames, per-field bytes, `line`/`column` at `9007199254740991`, and above-range protocol errors; and 262,144/262,145 structured-content bytes. Add distinct continued tests: `allThreadsContinued` omitted and true each clear all-target and every per-target eligibility; false with named A refuses A/no-write while B remains eligible; false without `threadId` fails closed, leaves no unknown token, and refuses all targets/no-write. Hold a correlated response across each all-thread continuation and across A continuation followed by A partial restop; reader-ordered publication must compare the request-local captured A token, refuse with no frames, preserve the session, and require a newly admitted request for A's replacement token. Retain explicit all-stop→partial-stop and target A resume→partial-restop checks: each partial stop clears every token and installs only A's supplied-target token; an omitted partial-stop `threadId` installs no token; target B receives no `stackTrace` write in every such state. Extend the controlled fixture only with modes necessary to make these checks RED. |
| B2-T03 | Code | B2-T02 | Implement only the additive typed native `stackTrace` operation and new MCP dispatch/catalog/result/schema variants required to turn B2-T02 green. In `NetCoreDbgSession`, capture only the initialize response's delayed-stack capability, use one internal DAP operation/state gate, and atomically replace eligibility on every stopped event: an all-stop installs one all-target token; a partial stop clears all all-target/per-target tokens and installs only the supplied thread token, or none when `threadId` is absent. Apply continued mechanically: omitted/true clears every token; false/named invalidates only that target while preserving unrelated eligibility; false/missing clears every token with no unknown retention. Capture the requested target's private token only for the request; reader-ordered response publication compares it to the still-current target token and returns refusal/no frames with the session preserved when continuation or resume→restop invalidated or replaced it. Do not expose or cache an epoch or eligibility; when capability is absent, no current token exists, or lifecycle transition wins admission/publication, return refusal with no `stackTrace` write or frames. Accept frame `line`/`column` only as integers `0..9007199254740991`; negative, non-integral, and above-range values are protocol errors. Keep the existing registry free of state/capability prechecks and use its `SessionSlot` lease/abort/close owner without a new registry or cleanup path. Normalize only id/name/source/line/column/optional totalFrames; accept no broader DAP frame/source authority. |
| B2-T04 | Review | B2-T03 | Obtain independent review of the exact B2 candidate. B2-G0 must already have issued its terminal recheck verdict; it is not reopened or substituted by this post-code review. B2 is S2: apply the installed platform `security-review` contract at `C:/Users/btf/.omp/profiles/nvmd-selfhost/plugins/cache/plugins/nvmd-ai___nvmd-ai___0.9.19/wiki/security-review.md`, covering closed input/token lookup, DAP request framing, initialize-capability/stopped/continued admission, all-thread/named-partial invalidation, request-local token-current response publication, response bounds/redaction, and cleanup. Write durable security evidence only to root `.agent/runs/b2-native-frame-inspection/security-review.md`. The implementation review must inspect no ambient thread, no generic forwarder/capability registry, atomic request-write admission, the all-thread/named-partial continuation cases, held-response no-frame/session-preserved behavior, safe-integer validation, redaction, lease/cleanup ordering, and no scope expansion. |
| B2-T05 | Test | B2-T04 | On the exact B2 candidate, run the focused B2 native process/typed-session/lifecycle/schema gates and, before or alongside them, run the current-candidate B1 baseline named `GetThreadsContractTests`, `SessionSlotContractTests`, `SessionSlotLifecycleTests`, `NetCoreDbgSessionTests`, and `StructuredContentSchemaParityTests`. Record each actual nonzero denominator; B1's pre-merge 52/52 receipt is not eligible evidence. Demonstrate the controlled native journey: start with delayed-stack capability, observe stopped state, `get_threads`, explicit `get_call_stack`, verify normalized page/no raw DAP content, verify session usability, then stop. Separately demonstrate capability-absent refusal/no-write; all-thread continuation omitted/true; named partial continuation preserves unrelated B; missing-thread partial continuation fails closed; held-response token-invalidated publication and resume→restop replacement both refuse/no-frame/session-preserved; partial-stop unrelated-target no-write behavior; and frame safe-integer boundary/protocol-error behavior. Replay the retained installed-Python non-selection journey only; do not change package, selector, configuration, release, or publication. |

## Fixture wiring

`ModernMcp/GetCallStackContractTests.cs` selects initialize-capability, held-response, all-thread continued omitted/true, named-partial continued, missing-thread partial-continued, and continued-before-restop response modes through `ModernMcpStartOptions.FixtureConfiguration`. `DebugAdapter/NetCoreDbgSessionContractDriver.cs` owns fixture configuration and `FixtureProcess.Create`, forwarding it through `CONTROLLED_DAP_OPTIONS`. `Fixtures/ControlledDapAdapter/Program.cs` owns option parsing, initialize capability response, stopped/continued event ordering, held `stackTrace` response release, and `stackTrace` mode behavior. `ModernMcp/ModernMcpProcessDriver.cs` remains the official MCP stdio process entry. These are future source/test scopes only; this planning packet executes none of them.

## Granularity check

T01 yields one written ownership/protocol finding, T02 yields RED observable behavior, T03 is one additive end-to-end code slice, T04 yields an independent S2 and challenger verdict, and T05 yields current-candidate customer-facing proof. Each is independently checkable and none absorbs a scopes, variables, Python migration, selector, tag, publication, or release slice.